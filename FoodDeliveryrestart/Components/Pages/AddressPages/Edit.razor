@page "/addresses/edit"
@using Microsoft.EntityFrameworkCore
@using FoodDeliveryrestart.Domain
@inject IDbContextFactory<FoodDeliveryrestart.Data.FoodDeliveryrestartContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Edit</PageTitle>

<h1>Edit</h1>

<h2>Address</h2>
<hr />
@if (Address is null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="row">
        <div class="col-md-4">
            <EditForm method="post" Model="Address" OnValidSubmit="UpdateAddress" FormName="edit" Enhance>
                <DataAnnotationsValidator />
                <ValidationSummary role="alert"/>
                <input type="hidden" name="Address.Id" value="@Address.Id" />
                <div class="mb-3">
                    <label for="block" class="form-label">Block:</label>
                    <InputText id="block" @bind-Value="Address.Block" class="form-control" />
                    <ValidationMessage For="() => Address.Block" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="unitnumber" class="form-label">UnitNumber:</label>
                    <InputText id="unitnumber" @bind-Value="Address.UnitNumber" class="form-control" />
                    <ValidationMessage For="() => Address.UnitNumber" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="street" class="form-label">Street:</label>
                    <InputText id="street" @bind-Value="Address.Street" class="form-control" />
                    <ValidationMessage For="() => Address.Street" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="postalcode" class="form-label">PostalCode:</label>
                    <InputText id="postalcode" @bind-Value="Address.PostalCode" class="form-control" />
                    <ValidationMessage For="() => Address.PostalCode" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="city" class="form-label">City:</label>
                    <InputText id="city" @bind-Value="Address.City" class="form-control" />
                    <ValidationMessage For="() => Address.City" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="userid" class="form-label">UserId:</label>
                    <InputNumber id="userid" @bind-Value="Address.UserId" class="form-control" />
                    <ValidationMessage For="() => Address.UserId" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="datecreated" class="form-label">DateCreated:</label>
                    <InputDate id="datecreated" @bind-Value="Address.DateCreated" class="form-control" />
                    <ValidationMessage For="() => Address.DateCreated" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="dateupdated" class="form-label">DateUpdated:</label>
                    <InputDate id="dateupdated" @bind-Value="Address.DateUpdated" class="form-control" />
                    <ValidationMessage For="() => Address.DateUpdated" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="createdby" class="form-label">CreatedBy:</label>
                    <InputText id="createdby" @bind-Value="Address.CreatedBy" class="form-control" />
                    <ValidationMessage For="() => Address.CreatedBy" class="text-danger" />
                </div>
                <div class="mb-3">
                    <label for="updatedby" class="form-label">UpdatedBy:</label>
                    <InputText id="updatedby" @bind-Value="Address.UpdatedBy" class="form-control" />
                    <ValidationMessage For="() => Address.UpdatedBy" class="text-danger" />
                </div>
                <button type="submit" class="btn btn-primary">Save</button>
            </EditForm>
        </div>
    </div>
}

<div>
    <a href="/addresses">Back to List</a>
</div>

@code {
    [SupplyParameterFromQuery]
    private int Id { get; set; }

    [SupplyParameterFromForm]
    private Address? Address { get; set; }

    protected override async Task OnInitializedAsync()
    {
        using var context = DbFactory.CreateDbContext();
        Address ??= await context.Address.FirstOrDefaultAsync(m => m.Id == Id);

        if (Address is null)
        {
            NavigationManager.NavigateTo("notfound");
        }
    }

    // To protect from overposting attacks, enable the specific properties you want to bind to.
    // For more information, see https://learn.microsoft.com/aspnet/core/blazor/forms/#mitigate-overposting-attacks.
    private async Task UpdateAddress()
    {
        using var context = DbFactory.CreateDbContext();
        context.Attach(Address!).State = EntityState.Modified;

        try
        {
            await context.SaveChangesAsync();
        }
        catch (DbUpdateConcurrencyException)
        {
            if (!AddressExists(Address!.Id))
            {
                NavigationManager.NavigateTo("notfound");
            }
            else
            {
                throw;
            }
        }

        NavigationManager.NavigateTo("/addresses");
    }

    private bool AddressExists(int id)
    {
        using var context = DbFactory.CreateDbContext();
        return context.Address.Any(e => e.Id == id);
    }
}
