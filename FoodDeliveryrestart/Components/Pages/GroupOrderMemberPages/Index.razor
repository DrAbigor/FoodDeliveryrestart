@page "/groupordermembers"
@using Microsoft.EntityFrameworkCore
@using Microsoft.AspNetCore.Components.QuickGrid
@using FoodDeliveryrestart.Domain
@using FoodDeliveryrestart.Data
@implements IAsyncDisposable
@inject IDbContextFactory<FoodDeliveryrestart.Data.FoodDeliveryrestartContext> DbFactory

<PageTitle>Index</PageTitle>

<h1>Index</h1>

<p>
    <a href="groupordermembers/create">Create New</a>
</p>

<QuickGrid Class="table" Items="context.GroupOrderMember">
    <PropertyColumn Property="groupordermember => groupordermember.JoinTime" />
    <PropertyColumn Property="groupordermember => groupordermember.Status" />
    <PropertyColumn Property="groupordermember => groupordermember.GroupOrderId" />
    <PropertyColumn Property="groupordermember => groupordermember.UserId" />
    <PropertyColumn Property="groupordermember => groupordermember.DateCreated" />
    <PropertyColumn Property="groupordermember => groupordermember.DateUpdated" />
    <PropertyColumn Property="groupordermember => groupordermember.CreatedBy" />
    <PropertyColumn Property="groupordermember => groupordermember.UpdatedBy" />

    <TemplateColumn Context="groupordermember">
        <a href="@($"groupordermembers/edit?id={groupordermember.Id}")">Edit</a> |
        <a href="@($"groupordermembers/details?id={groupordermember.Id}")">Details</a> |
        <a href="@($"groupordermembers/delete?id={groupordermember.Id}")">Delete</a>
    </TemplateColumn>
</QuickGrid>

@code {
    private FoodDeliveryrestartContext context = default!;

    protected override void OnInitialized()
    {
        context = DbFactory.CreateDbContext();
    }

    public async ValueTask DisposeAsync() => await context.DisposeAsync();
}
